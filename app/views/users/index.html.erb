<% provide(:title, '全てのユーザー') %>
<h1>全てのユーザー</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= image_tag user.picture, size: 50 %>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>
